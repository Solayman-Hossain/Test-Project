# Test-Project

Cursor প্রজেক্ট ফাইল। এই ওয়ার্কস্পেস: `e:\Documents\Test-Project`।

## প্রজেক্ট

- **নাম:** Test-Project
- **মালিক:** Solyman (Software engineer)
- **ভাষা:** বাংলা ও ইংরেজি — ইউজার বাংলায় কথা বললে বাংলায় উত্তর দাও
- **ওয়ার্কফ্লো:** সরাসরি কোডে কাজ, ডিবাগ, ইটারেট

## Tech Stack

- **Runtime:** Node.js
- **Language:** TypeScript (strict)
- **Framework:** Next.js (App Router)
- **UI:** React + TypeScript (`.tsx`)
- **Package manager:** npm

কোড লিখলে এই স্ট্যাক অনুসরণ করো: নতুন ফাইল `.ts` / `.tsx`, Next.js কনভেনশন (`app/`), এবং Node.js API যেখানে সার্ভার দরকার।

## এজেন্ট নির্দেশনা

- কোড পরিবর্তন স্পষ্ট ও ছোট রাখো; অনুরোধ না থাকলে অতিরিক্ত ফাইল তৈরি করো না
- Git commit শুধু ইউজার চাইলেই করো
- ওয়েব UI বদলালে ব্রাউজারে যাচাই করো; টুল না থাকলে কী যাচাই হয়নি বলো
- গোপন তথ্য (`.env`, কী, পাসওয়ার্ড) কমিট বা লগে দিও না

## স্ট্রাকচার

এখনো Next.js অ্যাপ স্ক্যাফোল্ড নেই। সেটআপের পর মূল ফোল্ডারগুলো:

- `app/` — Next.js App Router (পেজ, লেআউট, API routes)
- `components/` — রিইউজেবল React কম্পোনেন্ট
- `lib/` — হেল্পার ও সার্ভার ইউটিলিটি
- `public/` — স্ট্যাটিক অ্যাসেট
